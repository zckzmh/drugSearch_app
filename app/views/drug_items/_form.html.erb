<%= form_for(@drug_item) do |f| %>
  <% if @drug_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@drug_item.errors.count, "error") %> prohibited this drug_item from being saved:</h2>

      <ul>
      <% @drug_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
